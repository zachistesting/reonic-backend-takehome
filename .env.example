# Database connection string
# For local development (connecting to Docker PostgreSQL from host):
DATABASE_URL=postgresql://reonic:reonic_dev@localhost:54320/invoices

# Note: When running in Docker, the DATABASE_URL is automatically set in docker-compose.yml
# to use the internal Docker network: postgresql://reonic:reonic_dev@postgres:5432/invoices

# Server configuration (optional - customize as needed)
PORT=3000
NODE_ENV=development
